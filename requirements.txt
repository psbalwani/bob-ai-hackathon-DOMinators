# Track A (and later tracks) API/dev dependencies. Track C's generator is
# stdlib-only and doesn't need any of these.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.10
httpx>=0.27
pytest>=8.3
python-dotenv>=1.0

# Only imported lazily by src/detection/llm_hook.py when WATSONX_API_KEY is
# set; the hook falls back gracefully if this isn't installed.
ibm-watsonx-ai>=1.1
